"""浅度研判硬底线(确定性强制升级)。

★2026-07-23 退成**空底线**:原本按"高危技战术前缀(T1003/T1558…)"强制升级,但那些(Kerberoast/
DCSync/ADCS)其实都是**签名直判的 B 类** —— 升级到深度取证只补扇出/估值等细节、不改"是攻击"的
结论,强制升级反而挡住了"B 类浅层直接判"。故不再用技战术兜底,由浅层 LLM 按签名自己定
(见 [[soc-agent-cascade-openjiuwen]] 的提示词原则:只有深度取证能改变结论时升级才有意义)。

★注意:这也**撤掉了"绝不许浅层关掉高危告警"的召回兜底** —— 现在全靠浅层 LLM 的判断。真要针对
个别"漏了就灾难"的类型再加确定性兜底,在这里补(保留函数与签名做钩子)。
"""

__all__ = ["force_deep"]


def force_deep(alert, policy=None) -> bool:
    """当前空底线:不做确定性强制升级,升/不升全交浅层 LLM。保留钩子便于日后按需补。"""
    return False
