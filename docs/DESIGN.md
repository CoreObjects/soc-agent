# soc-agent 架构设计

> **状态：待定。** 计划在下一次工作会话（brainstorm）中产出。本文件是明天设计对齐的落点。

## 待对齐的关键决策

- **技术栈**：后端语言 / 框架、图数据库、LLM 接入方式、是否需要前端。
- **核心架构与分层**：检测 → 分流 → 研判 Agent → 经验沉淀 各层的取舍与边界。
- **本体建模**：告警事件 / 主机行为 / 网络访问 / 攻击模式等实体与关系；关联键（IP 无关）。
- **与 GOAD 真域数据源的对接**：日志 / 告警 schema；本体实体类型（域 / 域控 / 成员服务器 / MSSQL / ADCS / 信任关系等）。
- **覆盖的攻击链场景**：Kerberoast / 约束委派 / NTLM relay / MSSQL link / ADCS ESC 等（对齐 GOAD 真实攻击面）。
- **MVP 边界**：首个可演示闭环的最小范围。

## 参考

- 旧 SOC 系统作为设计参考，本仓库为**全新实现**（不迁移旧代码）。
