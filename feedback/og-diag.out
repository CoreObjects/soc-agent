=== openGauss 诊断(只读) 2026-08-13 15:23:38 ===
代码版本: d9e1b12 og-diag 三探:问题不是「高斯没起」,是**整个应用层重启后都没回来**
主机: localhost.localdomain   Linux 6.6.0-45.0.0.54.oe2409.aarch64 aarch64
开机: up 22 hours, 40 minutes
     最后启动:  system boot 2026-08-12 16:43

--- ① 我们到底在连什么(.env 里的 OG_* )---
  OG_HOST=127.0.0.1
  OG_PORT=5432
  OG_DATABASE=soc
  OG_USER=soc_agent
  OG_PASSWORD=***
  OG_SCHEMA=soc

--- ② 5432 上有没有人在听 ---
  State  Recv-Q Send-Q                          Local Address:Port  Peer Address:PortProcess
  ★**没有任何进程在听 5432** —— 这与 Connection refused 完全吻合,问题在①或③,不在网络。

--- ③ 进程在不在 ---
  (没有任何 gaussdb/postgres 进程)

--- ④ 装在哪、以谁的身份跑 ---
  gaussdb  (不在当前 PATH)
  gs_ctl   (不在当前 PATH)
  gs_om    (不在当前 PATH)
  gsql     (不在当前 PATH)
  GAUSSHOME=（当前 shell 未设,通常在 omm 用户的 profile 里）
  GAUSSDATA=（未设）   PGDATA=（未设）
  omm 用户: (没有 omm 用户)
  ★openGauss 一般以 **omm** 身份跑,当前用户 soc 的 PATH/env 里看不到很正常 ——
    别据此判断'没装'。下面直接去找数据目录与日志。

--- ⑤ systemd 里有没有它 ---
  (systemd 里没有相关 unit —— 那它多半是**手工起的**,重启就不会自己回来)
  is-enabled opengauss: not-found
  is-enabled gaussdb: not-found
  is-enabled openGauss: not-found

--- ⑥ 数据目录与锁文件(★残留 postmaster.pid 会让它起不来)---
  常见路径都没命中,全盘找一下(限时):
    ★全盘也没有 postgresql.conf —— 它多半**不是装在宿主机上的**(见下一节)
  数据目录 = （没找到）

--- ⑦ ★日志:它到底是怎么停的 / 为什么起不来 ---
  日志目录 = （没找到;可能在 omm 家目录下,需 sudo -u omm 看）

--- ⑦b ★它是不是跑在容器里 ---
  (首跑证据指向这个方向:**没有 omm 用户**、宿主机上没有任何 gaussdb 二进制,
   而 df 里出现了 /run/k3s/containerd/… —— 这台机器上有 k3s。
   我此前记的是「原生 openGauss」,这条得以实际证据为准。)
  k3s / containerd:
    k3s is-active: active
    k3s is-enabled: enabled
    k3s      /usr/local/bin/k3s
    kubectl  /usr/local/bin/kubectl
    crictl   /usr/local/bin/crictl
  KUBECONFIG=/etc/rancher/k3s/k3s.yaml  可读:否(要 sudo)
  ★所有 pod(不只找 gauss —— 要看的是**整个应用层回来没有**):
